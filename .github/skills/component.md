# Component Skill

- shadcn/ui only, no raw HTML
- No inline style props
- No aria attributes
- Use cn() for conditional classes
- Server Component by default
- Props as TypeScript interface above component
- Named export only, no default export